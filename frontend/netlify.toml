[build]
  base = "frontend"
  command = "ng build --configuration production"
  publish = "dist/frontend"

[build.environment]
  API_URL = "https://control-debts.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
